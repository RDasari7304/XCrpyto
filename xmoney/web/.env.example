# Local dev only. In production the frontend is served from the API's own
# origin, so both of these are left unset and default to same-origin.
VITE_API_URL=http://localhost:3000
VITE_RPC_URL=http://127.0.0.1:8899
